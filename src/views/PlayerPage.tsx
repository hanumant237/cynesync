"use client";

/**
 * CineSync — Player page (placeholder)
 *
 * Displays only the page title in this foundation phase.
 */

import { PageShell } from "@/components/layout/PageShell";

export function PlayerPage() {
  return <PageShell title="Player" />;
}
