/**
 * CineSync Backend — Shared Types
 *
 * Type definitions shared across the backend. Placeholder for future phases.
 */

// TODO (future phase): add backend domain types, e.g.
//   export interface WatchPartyRoom { id: string; ... }
