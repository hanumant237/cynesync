/**
 * CineSync Backend — Controllers
 *
 * Request handlers live here. Each controller function receives the Express
 * request/response objects and delegates business logic to the service layer.
 *
 * Placeholder for future phases.
 */

// TODO (future phase): add controllers, e.g.
//   export const mediaController = { list, getById, ... };
